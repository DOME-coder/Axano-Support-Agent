{
  "name": "@avatardesk/widget",
  "version": "0.0.0",
  "private": true,
  "description": "AvatarDesk embeddable widget — floating button + modal that streams a Beyond-Presence avatar via LiveKit.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not yet wired up for widget — phase 1'"
  },
  "dependencies": {
    "@avatardesk/shared": "workspace:*",
    "livekit-client": "^2.7.5",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
